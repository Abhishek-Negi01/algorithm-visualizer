export default {
  darkMode: "class",
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  // Add this if using custom layers:
  layers: {
    base: true,
    components: true,
    utilities: true,
  },
};
